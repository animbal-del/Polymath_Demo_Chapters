# OpenAI — powers both AI tutor responses (gpt-4o-mini) and voice narration (nova voice TTS)
# Get a key at: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# Without this key the app still works — it falls back to local hint text and browser TTS.
